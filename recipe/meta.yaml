{% set name = "blitzgsea" %}
{% set version = "1.3.49" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/blitzgsea-{{ version }}.tar.gz
  sha256: e4166411de7fd9877fe32d41cd110249050d55f51471b05125098990a78456be

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - pandas >=1.1.5
    - numpy
    - scikit-learn
    - tqdm
    - statsmodels
    - mpmath
    - matplotlib-base

test:
  imports:
    - blitzgsea
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/maayanlab/blitzgsea
  summary: Package for fast and accurate calculation of Gene Set Enrichment Analysis (GSEA) similar to prerank using gamma distribution approximation.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - wxicu
